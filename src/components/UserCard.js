import React from 'react';
import {Paper} from "@material-ui/core";
import { makeStyles } from '@material-ui/core/styles';

const UserCard = ({username, fullName, course, year}) => {

    return (
        <Paper>

        </Paper>
    )
}
export default UserCard